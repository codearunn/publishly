<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("./partials/head") %>
  <title>Create New Blog - Publishly</title>
  <style>
    body {
      background: linear-gradient(135deg, #F5E6D3 0%, #E8D5C4 100%);
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .create-blog-container {
      max-width: 800px;
      margin: 40px auto;
      padding: 0 20px;
    }

    .blog-form-card {
      background: #FFF8DC;
      border-radius: 20px;
      padding: 2.5rem;
      box-shadow: 0 8px 24px rgba(111, 78, 55, 0.15);
      border: 2px solid #D2B48C;
    }

    .form-header {
      text-align: center;
      margin-bottom: 2.5rem;
      padding-bottom: 1.5rem;
      border-bottom: 3px solid #D2B48C;
    }

    .form-header h1 {
      color: #6F4E37;
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }

    .form-header p {
      color: #8B6F47;
      font-size: 1.1rem;
      margin: 0;
    }

    .form-label {
      color: #6F4E37;
      font-weight: 600;
      font-size: 1.1rem;
      margin-bottom: 0.75rem;
      display: block;
    }

    .form-control,
    .form-select {
      background: #FFF8DC;
      border: 2px solid #D2B48C;
      border-radius: 12px;
      padding: 12px 16px;
      font-size: 1rem;
      color: #3E2723;
      transition: all 0.3s ease;
    }

    .form-control:focus,
    .form-select:focus {
      background: #FFF8DC;
      border-color: #6F4E37;
      box-shadow: 0 0 0 0.2rem rgba(111, 78, 55, 0.25);
      color: #3E2723;
      outline: none;
    }

    .form-control::placeholder {
      color: #A0826D;
      opacity: 0.7;
    }

    textarea.form-control {
      min-height: 360px;
      resize: vertical;
      font-family: 'Georgia', 'Times New Roman', serif;
      font-size: 1.05rem;
      line-height: 1.7;
      padding: 16px 18px;
      letter-spacing: 0.01em;
      background: #FFF4E6;
    }

    textarea.form-control:focus {
      background: #FFF8DC;
    }

    .file-input-wrapper {
      position: relative;
      display: inline-block;
      width: 100%;
    }

    .file-input-label {
      display: block;
      padding: 20px;
      background: #F5E6D3;
      border: 2px dashed #D2B48C;
      border-radius: 12px;
      text-align: center;
      cursor: pointer;
      transition: all 0.3s ease;
      color: #6F4E37;
      font-weight: 500;
    }

    .file-input-label:hover {
      background: #E8D5C4;
      border-color: #6F4E37;
    }

    .file-input-label.has-file {
      background: #D2B48C;
      border-color: #6F4E37;
      color: #3E2723;
    }

    input[type="file"] {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      border: 0;
    }

    .file-name-display {
      margin-top: 10px;
      color: #6F4E37;
      font-size: 0.9rem;
      font-weight: 500;
    }

    .form-group {
      margin-bottom: 2rem;
    }

    .btn-submit {
      background: linear-gradient(135deg, #6F4E37 0%, #8B4513 100%);
      color: #FFF8DC;
      border: none;
      padding: 15px 40px;
      font-size: 1.1rem;
      font-weight: 600;
      border-radius: 50px;
      width: 100%;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(111, 78, 55, 0.3);
      margin-top: 1rem;
    }

    .btn-submit:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(111, 78, 55, 0.4);
      background: linear-gradient(135deg, #8B4513 0%, #A0522D 100%);
      color: #FFF8DC;
    }

    .btn-submit:active {
      transform: translateY(0);
    }

    .form-icon {
      font-size: 1.2rem;
      margin-right: 8px;
      vertical-align: middle;
    }

    .help-text {
      color: #8B6F47;
      font-size: 0.9rem;
      margin-top: 0.5rem;
    }

    .character-count {
      text-align: right;
      color: #8B6F47;
      font-size: 0.85rem;
      margin-top: 0.5rem;
    }

    @media (max-width: 768px) {
      .create-blog-container {
        margin: 20px auto;
        padding: 0 15px;
      }

      .blog-form-card {
        padding: 1.5rem;
      }

      textarea.form-control {
        min-height: 240px;
      }

      .form-header h1 {
        font-size: 2rem;
      }
    }
  </style>
</head>
<body>
  <%- include("./partials/nav") %>

  <div class="create-blog-container">
    <div class="blog-form-card">
      <div class="form-header">
        <h1>☕ Create Your Blog</h1>
        <p>Share your thoughts and stories with the world</p>
      </div>

      <form method="post" action="/blog" enctype="multipart/form-data" id="blogForm">
        <div class="form-group">
          <label for="file" class="form-label">
            <span class="form-icon">🖼️</span>Cover Image
          </label>
          <div class="file-input-wrapper">
            <input type="file" id="file" name="coverImgURL" accept="image/*" required>
            <label for="file" class="file-input-label" id="fileLabel">
              <span>📎 Click to upload or drag and drop</span><br>
              <small style="font-size: 0.85rem; opacity: 0.8;">PNG, JPG, GIF up to 10MB</small>
            </label>
          </div>
          <div class="file-name-display" id="fileName" style="display: none;"></div>
          <div class="help-text">Choose an attractive cover image for your blog post</div>
        </div>

        <div class="form-group">
          <label for="title" class="form-label">
            <span class="form-icon">✍️</span>Blog Title
          </label>
          <input 
            type="text" 
            class="form-control" 
            id="title" 
            placeholder="Enter a captivating title for your blog..." 
            name="title"
            required
            maxlength="100"
          >
          <div class="character-count">
            <span id="titleCount">0</span>/100 characters
          </div>
        </div>

        <div class="form-group">
          <label for="body" class="form-label">
            <span class="form-icon">📝</span>Blog Content
          </label>
          <textarea 
            class="form-control" 
            id="body" 
            rows="10" 
            name="body"
            placeholder="Write your blog content here... Share your thoughts, stories, and ideas..."
            required
          ></textarea>
          <div class="help-text">Express yourself freely. There's no limit to creativity!</div>
        </div>

        <button type="submit" class="btn btn-submit">
          ☕ Publish Blog
        </button>
      </form>
    </div>
  </div>

  <script>
    // File input display
    const fileInput = document.getElementById('file');
    const fileLabel = document.getElementById('fileLabel');
    const fileName = document.getElementById('fileName');

    fileInput.addEventListener('change', function(e) {
      if (e.target.files.length > 0) {
        const fileNameText = e.target.files[0].name;
        fileLabel.classList.add('has-file');
        fileLabel.innerHTML = `✓ ${fileNameText}`;
        fileName.textContent = `Selected: ${fileNameText}`;
        fileName.style.display = 'block';
      } else {
        fileLabel.classList.remove('has-file');
        fileLabel.innerHTML = '<span>📎 Click to upload or drag and drop</span><br><small style="font-size: 0.85rem; opacity: 0.8;">PNG, JPG, GIF up to 10MB</small>';
        fileName.style.display = 'none';
      }
    });

    // Character counter for title
    const titleInput = document.getElementById('title');
    const titleCount = document.getElementById('titleCount');

    titleInput.addEventListener('input', function() {
      titleCount.textContent = this.value.length;
    });

    // Form validation with preventDefault
    document.getElementById('blogForm').addEventListener('submit', function(e) {
      const title = document.getElementById('title').value.trim();
      const body = document.getElementById('body').value.trim();
      const file = document.getElementById('file').files[0];
      
      if (!title) {
        e.preventDefault();
        alert('Please enter a title for your blog');
        document.getElementById('title').focus();
        return false;
      }
      
      if (!body) {
        e.preventDefault();
        alert('Please enter blog content');
        document.getElementById('body').focus();
        return false;
      }
      
      if (!file) {
        e.preventDefault();
        alert('Please select a cover image for your blog');
        document.getElementById('file').focus();
        return false;
      }
      
      // If all validation passes, form will submit normally
    });
  </script>

  <%- include("./partials/scripts") %>
</body>
</html>
