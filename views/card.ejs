<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("./partials/head") %>
  <title>Publishly</title>
  <style>
    body {
      background: linear-gradient(135deg, #F5E6D3 0%, #E8D5C4 100%);
      min-height: 100vh;
    }
    .blog-card {
      border: none;
      border-radius: 16px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1), 0 4px 8px rgba(0, 0, 0, 0.08);
      transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      overflow: hidden;
      background: #FFF8DC;
      position: relative;
      border: 2px solid #D2B48C;
    }
    .blog-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, #6F4E37, #8B4513, #A0522D);
      opacity: 0;
      transition: opacity 0.3s ease;
    }
    .blog-card:hover {
      transform: translateY(-12px) scale(1.02);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2), 0 10px 20px rgba(0, 0, 0, 0.15);
    }
    .blog-card:hover::before {
      opacity: 1;
    }
    .blog-card .card-img-top {
      border-radius: 16px 16px 0 0;
      transition: transform 0.4s ease;
    }
    .blog-card:hover .card-img-top {
      transform: scale(1.05);
    }
    .blog-card .card-body {
      padding: 1.5rem;
      background: linear-gradient(to bottom, #FFF8DC, #F5E6D3);
    }
    .blog-card .card-title {
      font-weight: 700;
      font-size: 1.25rem;
      color: #3E2723;
      margin-bottom: 1rem;
      line-height: 1.4;
      min-height: 3.5rem;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
    .blog-card .btn {
      border-radius: 25px;
      padding: 0.6rem 1.5rem;
      font-weight: 600;
      transition: all 0.3s ease;
      margin: 0.25rem;
    }
    .blog-card .btn-dark {
      background: linear-gradient(135deg, #6F4E37 0%, #8B4513 100%);
      border: none;
      box-shadow: 0 4px 15px rgba(111, 78, 55, 0.4);
    }
    .blog-card .btn-dark:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(111, 78, 55, 0.6);
    }
    .blog-card .btn-outline-danger {
      border: 2px solid #ef4444;
      color: #ef4444;
    }
    .blog-card .btn-outline-danger:hover {
      background: #ef4444;
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(239, 68, 68, 0.4);
    }
    .card-img-wrapper {
      position: relative;
      overflow: hidden;
      background: linear-gradient(135deg, #F5E6D3 0%, #D2B48C 100%);
    }
    .card-img-wrapper::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(to bottom, transparent 0%, rgba(0,0,0,0.1) 100%);
      opacity: 0;
      transition: opacity 0.3s ease;
    }
    .blog-card:hover .card-img-wrapper::after {
      opacity: 1;
    }
  </style>
</head>
<body>
  <%- include("./partials/nav") %>

  <div class="container-fluid mt-5">

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 row-cols-xl-4 g-4">
      <% (blogs || []).forEach(blog => { %>
        <div class="col">
          <div class="card h-100 d-flex flex-column blog-card">
            <div class="card-img-wrapper">
              <img src="/uploads/<%= blog.coverImgURL %>" class="card-img-top" alt="..." style="width: 100%; height: 250px; object-fit: contain; background-color: #f8f9fa;">
            </div>
            <div class="card-body d-flex flex-column">
              <h5 class="card-title"><%= blog.title%></h5>
              <div class="mt-auto d-flex flex-wrap gap-2">
                <a href="/blog/<%= blog._id%>" class="btn btn-dark" type="submit">
                  Read More
                </a>
                <a href="/blog/delete" class="btn btn-outline-danger"
                  onclick="return confirm('Are you sure you want to delete this blog?')">
                  Delete
                </a>
              </div>
            </div>
          </div>
        </div>
      <% }) %>
    </div>

  </div>

  <%- include("./partials/scripts") %>
</body>
</html>